version: '3.8'

services:
  agent-link:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - ADMIN_PASSWORD=AdminSecure2026!
    volumes:
      - ./data:/app/.data
    restart: unless-stopped
